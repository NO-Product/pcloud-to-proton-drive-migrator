version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    # Dependency updates are reviewed and applied manually to avoid automated PR history.
    open-pull-requests-limit: 0
